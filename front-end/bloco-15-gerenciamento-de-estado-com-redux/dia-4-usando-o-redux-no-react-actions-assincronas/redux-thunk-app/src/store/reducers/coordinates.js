const INITIAL_STATE = {
    latitude: 0,
    longitude: 0,
}

const coordinatesReducer = (state = INITIAL_STATE, action) => {
  switch(action.type) {
    default:
      return state;
  }
}

export default coordinatesReducer;